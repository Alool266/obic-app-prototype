// Made by Dr Ali
// Application roles — JWT `role` is a UI hint; sensitive paths re-check DB.

export enum UserRole {
  Customer = 'Customer',
  Employee = 'Employee',
  SuperAdmin = 'SuperAdmin',
}
